@model List<CrudClinica.Models.Medico>

@{
    ViewData["Title"] = "Médicos";
}

<div class="container py-5">


    @if (TempData["Sucesso"] != null)
    {
        <div class="alert alert-success alert-dismissible fade show shadow-sm"
             role="alert">

            <i class="bi bi-check-circle-fill me-2"></i>

            @TempData["Sucesso"]

            <button type="button"
                    class="btn-close"
                    data-bs-dismiss="alert"
                    aria-label="Fechar">
            </button>

        </div>
    }


    @if (TempData["Erro"] != null)
    {
        <div class="alert alert-danger alert-dismissible fade show shadow-sm"
             role="alert">

            <i class="bi bi-exclamation-triangle-fill me-2"></i>

            @TempData["Erro"]

            <button type="button"
                    class="btn-close"
                    data-bs-dismiss="alert"
                    aria-label="Fechar">
            </button>

        </div>
    }


    <div class="d-flex flex-column flex-md-row
                justify-content-between
                align-items-md-center
                gap-3
                mb-4">

        <div>

            <h1 class="fw-bold text-success mb-1">

                <i class="bi bi-person-badge me-2"></i>

                Médicos

            </h1>

            <p class="text-secondary mb-0">
                Gerenciamento dos médicos da clínica
            </p>

        </div>


        <a asp-controller="Medicos"
           asp-action="Cadastrar"
           class="btn btn-success">

            <i class="bi bi-person-plus-fill me-1"></i>

            Novo Médico

        </a>

    </div>


    <div class="card border-0 shadow rounded-4">

        <div class="card-body p-0">

            <div class="table-responsive">

                <table class="table table-hover table-striped
                              align-middle mb-0">

                    <thead class="table-dark">

                        <tr>

                            <th class="px-4">
                                ID
                            </th>

                            <th>
                                Nome
                            </th>

                            <th>
                                CRM
                            </th>

                            <th>
                                Especialidade
                            </th>

                            <th class="text-center">
                                Ações
                            </th>

                        </tr>

                    </thead>


                    <tbody>

                        @if (Model != null && Model.Any())
                        {
                            foreach (var medico in Model)
                            {
                                <tr>

                                    <td class="px-4 fw-semibold">
                                        @medico.idMedico
                                    </td>

                                    <td>
                                        @medico.nome
                                    </td>

                                    <td>
                                        @medico.crm
                                    </td>

                                    <td>
                                        @medico.especialidade
                                    </td>

                                    <td class="text-center">

                                        <!-- EDITAR -->

                                        <a asp-controller="Medicos"
                                           asp-action="Editar"
                                           asp-route-id="@medico.idMedico"
                                           class="btn btn-warning btn-sm me-1"
                                           title="Editar">

                                            <i class="bi bi-pencil-square"></i>

                                            Editar

                                        </a>


                                        <!-- EXCLUIR -->

                                        <form asp-controller="Medicos"
                                              asp-action="Excluir"
                                              asp-route-id="@medico.idMedico"
                                              method="post"
                                              class="d-inline">

                                            <button type="submit"
                                                    class="btn btn-danger btn-sm"
                                                    title="Excluir"
                                                    onclick="return confirm('Tem certeza que deseja excluir este médico?');">

                                                <i class="bi bi-trash3"></i>

                                                Excluir

                                            </button>

                                        </form>

                                    </td>

                                </tr>
                            }
                        }
                        else
                        {
                            <tr>

                                <td colspan="5"
                                    class="text-center py-5">

                                    <i class="bi bi-person-badge text-secondary"
                                       style="font-size: 3rem;">
                                    </i>

                                    <p class="text-secondary mt-3 mb-0">
                                        Nenhum médico cadastrado.
                                    </p>

                                </td>

                            </tr>
                        }

                    </tbody>

                </table>

            </div>

        </div>

    </div>

</div>